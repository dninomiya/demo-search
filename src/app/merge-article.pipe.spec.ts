import { MergeArticlePipe } from './merge-article.pipe';

describe('MergeArticlePipe', () => {
  it('create an instance', () => {
    const pipe = new MergeArticlePipe();
    expect(pipe).toBeTruthy();
  });
});
