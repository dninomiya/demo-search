import { MergeUserPipe } from './merge-user.pipe';

describe('MergeUserPipe', () => {
  it('create an instance', () => {
    const pipe = new MergeUserPipe();
    expect(pipe).toBeTruthy();
  });
});
